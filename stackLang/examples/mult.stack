#multiplies two elements, a,b
push input 0
push input 1
swap #a into temp
push 0
rotate #brings b to top, sum to bottom
while top
	swap #sum to top
	push @temp
	add #add a to sum
	push 1
	rotate 
	sub #subtract 1 from b
	.
pop output 1



